[tool.dagster]
module_name = "dagster_project.definitions"

[project]
name = "ecommerce-elt-pipeline"
version = "1.0.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
